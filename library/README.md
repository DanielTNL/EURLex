# Library Uploads

Place public reference documents for the app in `library/uploads/`.

Supported file types for indexing:
- `.pdf`
- `.txt`
- `.md`
- `.markdown`
- `.docx`

The GitHub backend will publish these files into `docs/data/library.json` for the app to read.
